---
deployment:
  tasks:
    - export DEPLOYPATH=/home/uxethegx/public_html/
    - /bin/cp index.html server.js package.json package-lock.json .htaccess $DEPLOYPATH
    - /bin/cp -R css $DEPLOYPATH
    - /bin/cp -R images $DEPLOYPATH
    - /bin/cp -R assets $DEPLOYPATH
    - cd $DEPLOYPATH && npm install --production